<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bayun">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".app.BayunApplication"
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:theme="@style/AppTheme">
        <activity android:name=".screens.RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screens.ListMessagesActivity"></activity>
        <activity android:name=".screens.ListExtensionActivity"></activity>
        <activity
            android:name=".screens.ConversationViewActivity"
            android:windowSoftInputMode="stateHidden|adjustResize"></activity>
    </application>

</manifest>
